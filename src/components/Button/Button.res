@react.component
let make = () => {
  open Utils

  <button
    className="bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 w-full rounded focus:outline-none">
    {string("Button")}
  </button>
}
